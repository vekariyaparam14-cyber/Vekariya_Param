# Task.4 :- Create a custom Python module called playlist_utils.py 
# with a function add_song(playlist, song) that adds a song to a list. Import this module in another script and use it to add three songs to a playlist, then print the final playlist.

def add_song(playlist,song):
    print(playlist +":"+ song)
    
add_song("love","Mane Malje")
